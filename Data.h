#include<iostream>
using namespace std;
class Data{
public:
       string data[1000];      
       int loc[100];
       int obcode[100];
       char load(fstream file){
             file.read(string, size);
       };                            
};
